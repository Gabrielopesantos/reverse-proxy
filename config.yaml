server:
  address: :8080
  read_timeout: 10

routes:
  /first:
    upstreams: ["localhost:8081"]
    middleware: []

  /second:
    upstreams: ["127.0.0.1:8082"]
    middleware: []

  /third:
    upstreams: ["127.0.0.1:8083"]
    middleware: []
